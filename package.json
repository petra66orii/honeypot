{
  "name": "honeypot-heroku",
  "private": true,
  "engines": {
    "node": ">=18 <21"
  },
  "scripts": {
    "heroku-postbuild": "npm --prefix honeypot-frontend install --include=dev && npm --prefix honeypot-frontend run build"
  }
}
